import React from 'react'

const SectionTitle = ({ title }) => {
    return (
        <h2 className='text-[30px] mt-8 mb-10 mx-auto text-center font-bold'>{title}</h2>
    )
}

export default SectionTitle
